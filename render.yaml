services:
  - type: web
    name: real-firefox-browser
    env: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PORT
        value: 10000
    plan: starter  # May need starter plan for VNC
    autoDeploy: true
